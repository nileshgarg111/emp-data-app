=========================
To install all modules

npm install

=========================

To start the server run below command
   NODE_ENV=local npm start
==========================
Post used in code : 5001

========================= Postman for getData =====================
